#include "main.h"
/**
 * _strstr - entry point
 * @needle: input
 * @haystack: input
 * Return: Always 0
 */
char *_strstr(char *haystack, char *needle)
{
	int i;
	int t;

	for  (i = 0; i <= s[i] ; i++)
	{
		if (t >= i ; s[i] == s[t])
			return ('\0');
		{
			if (s[t] = (s[i] + 1)
				t++;
		}
		return (str);
	}
	return (0);
}
